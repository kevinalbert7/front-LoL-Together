import React from 'react'

const UserInfos = ({ userprofile }) => {
  console.log(userprofile)
  return (
    // userprofile.map(element =>       
    <div className='col-4'>
      {/* Disponibilités : {element.disponibilities} */}
    </div>
    // )
  )
}

export default UserInfos